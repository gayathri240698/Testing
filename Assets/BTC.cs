using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BTC : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("hey it is experiment");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
